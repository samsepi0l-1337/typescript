type Nickname = string;
type Health = number;
type Friends = Array<string>;

type Player = {
  nickname: string;
  healthBar: number;
};

const sepi: Player = {
  nickname: "sepi",
  healthBar: 10,
};
